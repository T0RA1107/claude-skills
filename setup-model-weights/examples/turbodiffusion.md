# Example: TurboDiffusion

Whole-dir style: the entire `checkpoints/` directory is artifacts, so it is linked whole.

## Link structure
```
/home/aolab/Desktop/TurboDiffusion/checkpoints -> /data/TurboDiffusion/checkpoints
```
