# Example: OmniWeaving

Whole-dir style: the entire `checkpoints/` directory is artifacts, so it is linked whole.

## Link structure
```
/home/aolab/Desktop/OmniWeaving/checkpoints -> /data/OmniWeaving/checkpoints
```
